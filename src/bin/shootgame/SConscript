Import('env')
envc = env.Clone()

progname = 'shootgame'

envc.Program(progname, 'main.cpp')
envc.Append(LIBS=['ShootGame'])

if envc['PLATFORM'] == 'posix':
    envc.Install('#inst/bin', progname)
elif envc['PLATFORM'] == 'win32':
    envc.Install('#inst/bin', '%s.exe' % progname)
